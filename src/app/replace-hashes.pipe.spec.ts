import { ReplaceHashesPipe } from './replace-hashes.pipe';

describe('ReplaceHashesPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceHashesPipe();
    expect(pipe).toBeTruthy();
  });
});
